// TODO: this assignment asked you to actually create a new String (i.e. define it in a variable), and not just print a result. Also, you never checked whether it already started with 'not', as discussed in the instructions
public class NegatePhrase {
    public static void main(String[] args) {
        String sentence = args[0];
        System.out.println("not " + sentence);
    }
}
